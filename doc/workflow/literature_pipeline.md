# Literature Pipeline

1. Put raw WoS exports in `data/raw/wos/`.
2. Run merge and cleaning utilities from your local literature-processing workspace.
3. Store curated paper lists in `data/processed/datasets/` or another tracked output location.
4. Version retrieval and screening prompts in `prompt/wos/`.
5. Record query changes in commit messages and prompt filenames.
